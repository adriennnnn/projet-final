<div class="grid grid-cols-12 m-10">
    <div class="col-span-6 bg-white p-10">
        {{ form_start(form) }}
            {# {{ form_widget(form) }} #}
            {# {{ form_row(form.companyName) }} #}
                <div class="grid grid-cols-4">
                    <div class="col-span-2">
                        <div class="form-control pr-4">
                            {{ form_label(form.companyName, 'Nom de la companie', {'label_attr': {'class': 'foo'}}) }}
                            <br>
                            {{ form_widget(form.companyName, {'attr': {'class': 'lable_company_size'}}) }}    
                        </div>
                {# {{ form_row(form.siretNumber) }} #}
                        <div class="form-control pr-4 ">
                            {{ form_label(form.siretNumber, 'Numero Siret', {'label_attr': {'class': 'foo'}}) }}
                            <br>
                            {{ form_widget(form.siretNumber, {'attr': {'class': 'lable_company_size'}}) }}    
                        </div>
                    </div>
            {# {{ form_row(form.companyDescription) }} #}
                    <div class="col-span-2">
                        <div class="form-control">
                            {{ form_label(form.companyDescription, 'Description de la companie', {'label_attr': {'class': 'foo'}}) }}
                            <br>
                            {{ form_widget(form.companyDescription, {'attr': {'class': 'lable_company_size'}}) }}    
                        </div>
                    </div> 
            {# {{ form_row(form.userId, )    }} #}
                </div>

            <button class=" w-full mt-10 text-purple-400 border-solid border-2 border-purple-400">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
    <div class="col-span-6 bg-purple-400">
        <p>Vous etes connectez en avec l'adresse email;</p>
        <p>{{app.user.userIdentifier}}</p>
    </div>
</div>
