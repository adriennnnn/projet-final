{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="">

            <ul class="bg-gray-50 border-solid border-y-2 border-slate-100 px-10 pb-[3px] pt-[5px]" >
            {% for show_case_category in categories %}
                <li class="inline-block px-2 py-[1px] border-solid border-2 border-gay-900 rounded-lg ">
                    <a href="">
                    {{ show_case_category|capitalize }}
                    </a>
                </li>

    {% endfor %}
            </ul>

    </div>
    <div>
        {% for show_case in show_cases %}
            {# <tr>
                <td>{{ show_case.id }}</td>
                <td>{{ show_case.title }}</td>
                <td>{{ show_case.description }}</td>
                <td><image src='../public/upload/{{ show_case.imageShowCases }}'></td>
app_show_case_show-index
                <td>
                    <a href="{{ path('app_show_case_show', {'id': show_case.id}) }}">show</a>
                    <a href="{{ path('app_show_case_edit', {'id': show_case.id}) }}">edit</a>
                </td>
            </tr> #}
            <div class="flex justify-center mx-44 py-4">
                <a href="{{ path('app_show_case_show', {'id': show_case.id}) }}" class="flex flex-col items-center bg-white w-full rounded-lg border shadow-md md:flex-row md:max-w-xl hover:bg-gray-100 dark:border-gray-700 dark:bg-gray-800 dark:hover:bg-gray-700">
                    {% if show_case.imageShowCases.values %}
                        <img class="object-cover rounded-t-lg md:h-auto md:w-48 md:rounded-none md:rounded-l-lg h-48 w-96" src="{{ asset('uploads/' ~ show_case.imageShowCases.values[0].image)}}" alt="">
                    {% endif %}
                    <div class="flex flex-col justify-between p-4 leading-normal">
                        <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{ show_case.title }}</h5>
                        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">{{ show_case.description }}</p>
                    </div>
                </a>
            </div>




        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
    </div>

    <div>
        <h1 class="text-white flex justify-center text-4xl mt-10 mb-5 font-bold">Bienvenue sur GlobalMarket</h1>
        <p class="text-white flex justify-center mb-20">votre référant des commerces et vendeurs de detaille en tous genres autour de chez vous</p>

    </div>





    {# <div>
        {% if ShowCase.image %}
            <a href="{{ ShowCase }}">
                <img>
            </a>
        {% endif %}
    </div>
    <div class="col-md-7">
        <h2>
            <a class="text-decoration-none" href=""></a>
        </h2>
    </div> #}
            {# <tbody>
        {% for show_case in show_cases %}
            <tr>
                <td>{{ show_case.id }}</td>
                <td>{{ show_case.title }}</td>
                <td>{{ show_case.description }}</td>
                <td>
                    <a href="{{ path('app_show_case_show', {'id': show_case.id}) }}">show</a>
                    <a href="{{ path('app_show_case_edit', {'id': show_case.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody> #}


{% endblock %}
